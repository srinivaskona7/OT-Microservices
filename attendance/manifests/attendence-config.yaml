apiVersion: v1
kind: ConfigMap
metadata:
  name: attendance-config
data:
  config.yaml: |-
    attendance:
      api_port: "8081"
    mysql:
      db_name: "attendance_db"
      host: "empms-db-service" # Update to use the service name
      username: "root"
      password: "password"
